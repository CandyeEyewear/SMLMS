#
# Monorepo note:
# - The Next.js app lives in `apps/web`.
# - For local dev, create `apps/web/.env.local` and set values there.
#
# This root file is kept as a reference; see `apps/web/.env.example`.
#
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
