#
# Supabase public configuration for the Next.js web app.
#
# IMPORTANT (monorepo):
# - Next.js loads env files from the app directory (`apps/web`).
# - Create `apps/web/.env.local` and set values there for local development.
#

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only (never expose this to the browser)
SUPABASE_SERVICE_ROLE_KEY=

